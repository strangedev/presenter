# 5. Code

```ts
const foobar: () => number = () => {
  return () => 42;
};

type Bubatz = 420 | ':thumbsup:';

const vegan: () => string = () => {
  return () => 'nicht kevin';
};

type Kevin = 'nicht vegan';
```
